% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_neutral_recruitment.R
\name{spesim_simulate_neutral_recruitment}
\alias{spesim_simulate_neutral_recruitment}
\title{Spatially-explicit neutral community simulation with convergence checks}
\usage{
spesim_simulate_neutral_recruitment(poly, P)
}
\arguments{
\item{poly}{An \code{sf} polygon defining the sampling domain.}

\item{P}{A fully materialized parameter list from \code{load_config()}. It must
also contain convergence parameters: \code{NEUTRAL_MAX_STEPS},
\code{NEUTRAL_CONVERGENCE_INTERVAL}, \code{NEUTRAL_CONVERGENCE_THRESHOLD},
\code{NEUTRAL_CONVERGENCE_PATIENCE}.}
}
\value{
An \code{sf} POINT layer with species and environmental data.
}
\description{
Simulates a spatially-explicit neutral or hybrid community model, running
until the community composition converges or a maximum number of steps is
reached. This is an internal function called by
\code{generate_heterogeneous_distribution} when the model family is set to
\code{"neutral_hubbell_like"} or \code{"hybrid"}.
}
\details{
The simulation follows a Moran-style process of death, followed by recruitment.
Recruitment can be from a local parent (with dispersal limitation) or from an
external metacommunity.

\strong{Convergence:}
Instead of running for a fixed number of steps, this implementation checks
for convergence. At intervals specified by \code{NEUTRAL_CONVERGENCE_INTERVAL},
it calculates the Bray-Curtis dissimilarity between the current community's
species abundance vector and the vector from the previous check. If this
dissimilarity is below \code{NEUTRAL_CONVERGENCE_THRESHOLD} for a number of
consecutive checks equal to \code{NEUTRAL_CONVERGENCE_PATIENCE}, the community
is considered to have reached a stationary state, and the simulation stops.
A \code{NEUTRAL_MAX_STEPS} parameter prevents infinite loops.
}
\keyword{internal}
