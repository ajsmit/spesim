% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_points_geyer_fast.R
\name{simulate_points_geyer_fast}
\alias{simulate_points_geyer_fast}
\title{Fast Geyer saturation simulator (Rcpp backend; polygon-clipped)}
\usage{
simulate_points_geyer_fast(
  domain,
  n_target,
  r,
  gamma,
  sat,
  sweeps = 2000,
  burnin = 200,
  thin = 1
)
}
\arguments{
\item{domain}{sf polygon/multipolygon defining the window.}

\item{n_target}{integer, number of points to return.}

\item{r}{interaction radius.}

\item{gamma}{interaction parameter (<1 inhibition, >1 clustering).}

\item{sat}{saturation count (positive integer).}

\item{sweeps}{MH sweeps (per point).}

\item{burnin}{burn-in sweeps.}

\item{thin}{unused (reserved).}
}
\value{
\code{sf} POINT layer with \code{n_target} points.
}
\description{
Runs a fixed-n Metropolis–Hastings sampler in the domain bounding box and
then clips points to the polygon.

This implementation is \strong{heuristic but internally consistent} (see C++
header): it targets a saturation potential based on per-point neighbour counts.
It is \strong{not} a drop-in reproduction of the exact
\pkg{spatstat} Geyer saturation process likelihood.

The result is thinned to return exactly \code{n_target} points. If clipping leaves
too few points, we generate additional points (never by duplicating existing ones).
}
