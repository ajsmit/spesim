% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_points_strauss_fast.R
\name{simulate_points_strauss_fast}
\alias{simulate_points_strauss_fast}
\title{Fast Strauss simulator (Rcpp backend) returning an sf point layer}
\usage{
simulate_points_strauss_fast(
  domain,
  n_target,
  r,
  gamma,
  sweeps = 2000,
  burnin = 200,
  thin = 1,
  step0 = NA_real_,
  target_acc = 0.35,
  tune_every = 200
)
}
\arguments{
\item{domain}{An \pkg{sf} polygon/multipolygon defining the window.}

\item{n_target}{Integer; number of points to return.}

\item{r}{Interaction radius (map units).}

\item{gamma}{Interaction parameter (\eqn{\le 1} for inhibition).}

\item{sweeps}{Number of MH sweeps (proposals per point). Default \code{2000}.}

\item{burnin}{Number of initial sweeps reserved for adaptation. Default \code{200}.}

\item{thin}{Currently unused (kept for API symmetry). Default \code{1}.}

\item{step0}{Optional initial proposal radius (defaults to \code{0.5 * r}).}

\item{target_acc}{Target acceptance for auto-tuning. Default \code{0.35}.}

\item{tune_every}{Sweeps between tuning updates. Default \code{200}.}
}
\value{
An \pkg{sf} POINT layer with \code{n_target} rows.
}
\description{
Fixed-\eqn{n} Metropolis–Hastings sampler for the Strauss process
with interaction radius \code{r} and parameter \code{gamma} (\eqn{0<\gamma\le 1}).
Uses a cell grid, cached neighbor counts, squared-distance checks, and
light auto-tuning for the proposal radius. This is a fast, self-contained
alternative to spatstat for baseline point generation.
}
