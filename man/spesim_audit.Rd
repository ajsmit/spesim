% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit.R
\name{spesim_audit}
\alias{spesim_audit}
\title{Audit whether a spesim run matches the intended qualitative regime}
\usage{
spesim_audit(res, species = "all", nn_k = 1, diagnostics = c("nn", "spatstat"))
}
\arguments{
\item{res}{A \code{spesim_result} list returned by \code{\link[=spesim_run]{spesim_run()}} (preferred) or
\code{\link[=run_spatial_simulation]{run_spatial_simulation()}} (legacy).}

\item{species}{Character vector of species labels to audit for spatial
structure. Default \code{"all"} audits all species present.}

\item{nn_k}{Integer; the neighbour order used for NN distances. Default 1.}

\item{diagnostics}{Character vector controlling which diagnostics to compute.
Default \code{c("nn", "spatstat")} will try to compute both nearest-neighbour
and spatstat-based diagnostics; if spatstat is not installed, those
diagnostics are skipped with a message. Use \code{"nn"} only for a lightweight
audit.}
}
\value{
A list of class \code{spesim_audit} with components:
\itemize{
\item \code{spatial}: data.frame of NN diagnostics per species
\item \code{spatial_spatstat}: data.frame of edge-corrected diagnostics (if requested)
\item \code{filtering}: data.frame of environment-abundance checks (may be empty)
\item \code{sampling}: a list describing quadrat placement rejection/exclusion metrics
\item \code{regime}: green/amber/red classification (see \code{\link[=spesim_regime]{spesim_regime()}})
}
}
\description{
spesim is designed for teaching and method testing: you often want to know
whether the \emph{realised} simulation actually exhibits the qualitative regime you
think you requested (e.g., clustering vs inhibition; strong vs weak
environmental filtering; heavy boundary exclusion in a sampling scheme).

\code{spesim_audit()} computes lightweight, dependency-minimal diagnostics that can
be used as a conceptual "tutor": it reports what the simulator instantiated,
and quantifies key patterns.
\itemize{
\item \strong{Spatial structure:} nearest-neighbour (NN) diagnostics per species,
comparing the observed mean NN distance to the CSR expectation.
\item \strong{Environmental filtering:} checks whether quadrat abundances for
gradient-responsive species peak near their configured optima.
\item \strong{Sampling design:} reports boundary exclusion / rejection rates for the
chosen quadrat placement scheme (when available).
}

For more formal point-process diagnostics (Ripley's K, pair correlation
functions, edge corrections in irregular domains), use \code{spatstat} tooling on
the exported point patterns.
}
