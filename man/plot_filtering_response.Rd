% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_filtering.R
\name{plot_filtering_response}
\alias{plot_filtering_response}
\title{Plot environmental filtering responses (sanity check)}
\usage{
plot_filtering_response(res, species = "all", add_smooth = TRUE)
}
\arguments{
\item{res}{A \code{spesim_result} list returned by \code{\link[=spesim_run]{spesim_run()}}.}

\item{species}{Species labels to plot. Default \code{"all"} uses all
gradient-responsive species in \code{res$P$GRADIENT}.}

\item{add_smooth}{Logical; add a loess smooth (teaching-friendly). Default
\code{TRUE}.}
}
\value{
A ggplot object.
}
\description{
A teaching/method-testing helper that visualises whether a gradient-responsive
species shows higher abundance in quadrats whose environments are near the
configured optimum (with tolerance band).

This plot is designed to pair with \code{\link[=audit_environmental_filtering]{audit_environmental_filtering()}}. It is
intentionally simple: it uses per-quadrat mean environments (as computed by
\code{\link[=calculate_quadrat_environment]{calculate_quadrat_environment()}}) and the site x species abundance matrix.
}
