% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regime.R
\name{spesim_regime}
\alias{spesim_regime}
\title{Classify whether a spesim run matches the intended qualitative regime}
\usage{
spesim_regime(res, audit = NULL, thresholds = list())
}
\arguments{
\item{res}{A \code{spesim_result} list (as returned by \code{\link[=spesim_run]{spesim_run()}}).}

\item{audit}{Optional list returned by \code{\link[=spesim_audit]{spesim_audit()}}. If \code{NULL}, the
function will compute an audit internally.}

\item{thresholds}{Optional named list overriding default thresholds. See
Details.}
}
\value{
A named list with sub-lists \code{spatial}, \code{filtering}, \code{sampling}. Each
contains:
\itemize{
\item \code{grade}: one of \code{"green"}, \code{"amber"}, \code{"red"}, \code{"unknown"}
\item \code{reason}: short explanation
\item \code{metrics}: compact numeric context
}
}
\description{
This helper turns the numeric outputs of \code{\link[=spesim_audit]{spesim_audit()}} into a compact,
teaching-friendly judgement (green/amber/red) about whether the realised
simulation matches the qualitative regime the user believes they are
simulating.

The defaults are deliberately conservative: when sample sizes are too small
or signals are weak, the result tends toward \strong{amber} rather than over-
confident green/red.
}
\details{
Default thresholds (you can override via \verb{thresholds=}):
\itemize{
\item Spatial NN ratio vs CSR (per species; uses dominant A if present):
\itemize{
\item \code{clustered}: nn_ratio < 0.85
\item \code{CSR_like}: 0.85 ≤ nn_ratio ≤ 1.15
\item \code{inhibited}: nn_ratio > 1.15
\item minimum points for classification: \code{min_n = 50}
}
\item Filtering (per gradient-responsive species):
\itemize{
\item minimum occupied sites: \code{min_occ = 5}
\item \code{good}: Spearman rho(-|z|, abundance) > 0.2 AND slope(abund~z) < 0
\item \code{opposite}: rho < -0.2 OR slope > 0
}
\item Sampling constraint:
\itemize{
\item if boundary exclusion estimate > 50\% → amber/red (scheme-dependent)
\item if random placement attempts are close to the cap (≥90\% of max attempts)
→ amber
}
}
}
