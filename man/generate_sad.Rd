% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sad.R
\name{generate_sad}
\alias{generate_sad}
\title{Generate a species abundance distribution (SAD)}
\usage{
generate_sad(n_species, n_individuals, model = "fisher", sad = NULL, ...)
}
\arguments{
\item{n_species}{Integer (>= 1). Number of species.}

\item{n_individuals}{Integer (>= 0). Number of individuals.}

\item{model}{Character scalar. One of the models described under
\dQuote{Models} in \code{?sad_generators}.}

\item{sad}{For \code{model = "custom"}: either a numeric vector (probabilities or
counts) or a function \verb{function(n_species, n_individuals, ...)} returning a
numeric vector.}

\item{...}{Model-specific parameters. Common arguments include:
\code{k} (geometric), \code{exponent} and \code{q} (Zipf / Mandelbrot), \code{meanlog}, \code{sdlog}
(lognormal), \code{shape}, \code{rate} (Poisson--gamma), and neutral-theory \code{theta},
\code{m}.

For \code{model = "fisher"}, pass \code{dominant_fraction}, \code{alpha}, and \code{x} (see
\code{\link[=generate_fisher_log_series]{generate_fisher_log_series()}}).}
}
\value{
A named integer vector of length \code{n_species} (some entries may be 0)
whose names are \code{LETTERS[1:n_species]}.
}
\description{
Returns a named vector of integer abundances for \code{n_species} that sums to
\code{n_individuals}.
}
