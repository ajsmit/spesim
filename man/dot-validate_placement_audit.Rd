% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quadrats_audit.R
\name{.validate_placement_audit}
\alias{.validate_placement_audit}
\title{Validate a placement_audit attribute}
\usage{
.validate_placement_audit(x, stop_on_error = FALSE)
}
\arguments{
\item{x}{An object with (optional) \code{attr(x, "placement_audit")}.}

\item{stop_on_error}{Logical; stop on validation errors? Default \code{FALSE}.}
}
\value{
A normalised \code{placement_audit} list (possibly empty).
}
\description{
Quadrat placement functions in spesim attach a \code{placement_audit} attribute to
the returned \code{sf} object. This is used by \code{\link[=audit_sampling_scheme]{audit_sampling_scheme()}} and the
report's conceptual audit section.

This validator is intentionally permissive: it checks for a minimal common
schema and normalises field types where possible, but it does not error on
scheme-specific fields.
}
\keyword{internal}
