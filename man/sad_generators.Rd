% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sad.R
\name{sad_generators}
\alias{sad_generators}
\title{Species abundance distribution (SAD) generators}
\description{
Utilities to generate \strong{species abundance distributions} (SADs) for a
community of \code{n_species} and \code{n_individuals}.

In spesim, SADs are used to create a per-species abundance vector (named
\code{A}, \code{B}, ...), after which individuals are placed in space.
}
\details{
Most classic SADs are defined as a probability vector
\eqn{p_1,\ldots,p_S} over species ranks, then sampled to integer counts via
\code{rmultinom(1, n_individuals, prob = p)}.

Some sampling-model SADs (e.g. Poisson--lognormal, Poisson--gamma) are
implemented by first simulating latent rates and then drawing counts. Because
spesim's downstream spatial simulation typically expects exactly
\code{n_individuals} points, these generators rescale/adjust to sum exactly to
\code{n_individuals}.
}
\section{Models}{

\code{generate_sad()} supports (case-insensitive):
\describe{
\item{\code{"fisher"}}{Fisher log-series with an explicit dominant species via
\code{\link[=generate_fisher_log_series]{generate_fisher_log_series()}}.}
\item{\code{"geometric"}}{Geometric series (niche pre-emption / Motomura).}
\item{\code{"brokenstick"}}{Broken stick (MacArthur).}
\item{\code{"zipf"}}{Zipf rank-abundance: \eqn{p_i \propto i^{-a}}.}
\item{\code{"zipf-mandelbrot"}}{Zipf--Mandelbrot:
\eqn{p_i \propto (i + q)^{-a}}.}
\item{\code{"lognormal"}}{Lognormal weights + multinomial sampling.}
\item{\code{"poisson-lognormal"}}{Poisson--lognormal sampling model (rates are
lognormal; counts are Poisson).}
\item{\code{"poisson-gamma"}}{Poisson--gamma sampling model (rates are Gamma;
counts are Poisson). Closely related to negative-binomial mixtures.}
\item{\code{"zsm"}}{Neutral-theory SAD helper. With \code{theta} only, this uses a
theta-only Ewens sampler (Chinese restaurant) to control the
\strong{rank--abundance} curve. If an immigration probability
\eqn{m \in (0,1)} is supplied, spesim uses a simple neutral
\emph{death--birth with immigration} heuristic (Moran style) to make the
SAD more uneven as \code{m} decreases. This is a pragmatic SAD helper and
is not a full neutral dynamics engine (spatial patterns are controlled
separately by the point-process settings). \strong{Important notes:}
(1) The immigration-modulated model runs until convergence is detected
(monitoring the abundance vector) or \code{max_steps} is reached.
(2) The simulation may produce more or fewer species than \code{n_species};
the resulting list is truncated or padded with zeros, and the total
number of individuals is adjusted to match \code{n_individuals} using a
proportional allocation method, which avoids lumping tail-end abundances
into a single species.}
\item{\code{"custom"}}{User-supplied SAD via a numeric vector or a function
(\code{sad} argument).}
}
}

