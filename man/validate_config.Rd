% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/usability.R
\name{validate_config}
\alias{validate_config}
\title{Validate a spesim configuration (without running the simulation)}
\usage{
validate_config(config, interactions_file = NULL, strict = FALSE)
}
\arguments{
\item{config}{Either a path to an init file (character scalar) or an in-memory
parameter list \code{P}.}

\item{interactions_file}{Optional interactions file path; if provided, it is
validated as well.}

\item{strict}{Logical; if \code{TRUE}, treat warnings as errors.}
}
\value{
A list with components \code{ok} (logical), \code{errors} (character),
\code{warnings} (character), and (when possible) \code{P} (resolved config).
}
\description{
Checks a configuration file or parameter list for common problems and returns
a structured report (errors + warnings) without stopping.

This is aimed at teaching workflows where students iterate on init files and
need fast feedback before running a full simulation.
}
