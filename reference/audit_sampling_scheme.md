# Audit quadrat placement: boundary exclusion and rejection metrics

Audit quadrat placement: boundary exclusion and rejection metrics

## Usage

``` r
audit_sampling_scheme(quadrats)
```

## Arguments

- quadrats:

  An `sf` object returned by a `place_quadrats_*()` function.

## Value

A list; empty if no audit info is attached.
