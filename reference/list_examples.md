# List shipped example configuration files

Prints (and returns) the example init files shipped in `inst/examples/`.

## Usage

``` r
list_examples()
```

## Value

A tibble with columns `name` and `path`.
