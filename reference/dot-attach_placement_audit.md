# Attach a placement_audit attribute to a quadrats sf object

Attach a placement_audit attribute to a quadrats sf object

## Usage

``` r
.attach_placement_audit(quadrats, audit)
```

## Arguments

- quadrats:

  sf object.

- audit:

  list.

## Value

quadrats with normalised `placement_audit` attribute.
